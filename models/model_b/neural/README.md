# This directory stores neural (BERT/T5) checkpoints for Model B (distractor/hint).
# Neural models were not trained in this project submission.
# Distractor and Hint generation is implemented using Classical ML (frequency-based
# candidate extraction + keyword-overlap heuristics) as per the rubric.
